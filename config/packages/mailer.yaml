framework:
    mailer:
        dsn: '%env(MAILER_DSN)%'
        envelope:
            sender: '%env(MAILER_SENDER_ADDRESS)%'
        headers:
            From: '%env(MAILER_SENDER_NAME)% <%env(MAILER_SENDER_ADDRESS)%>'
